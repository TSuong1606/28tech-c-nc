#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;

int main() 
{ fast;
    int n,m;
    cin>>n>>m;
    set<int> s,a,b;
    for(int i=0; i<n; ++i) {
      int x; cin>>x;
      s.insert(x);
      a.insert(x);
    }
    for(int i=0; i<m; ++i) {
      int x; cin>>x;
      a.insert(x);
      b.insert(x);
    }  
    for(auto it : s) {
      if(b.count(it)==0) b.erase(it);
    }
    for(auto it : b) if (s.count(it)==0) b.erase(it);
    for(auto it : b) cout<<it<<' ';
    cout<<endl;
    for(auto it : a) cout<<it<<' ';
    return 0;
}
//44:20-1:16:00 :tìm hợp và giao của hai mảng (có đoạn chữa bài cho học sinh)
