#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;

int main() 
{ fast;
  int n;
  cin>>n;
  set<pair<int,int>> s;
  for(int i=0; i<n; ++i){
    int x,y;
    cin>>x>>y;
    if (x>y) swap (x,y);
    s.insert({x,y});
    //if (s.count({y,x})) s.erase ({y,x});
  }
  cout<<s.size()<<endl;
  for(auto it : s) cout<<it.first<<' '<<it.second<<endl;
    return 0;
}
1:51:34 off video 13th
